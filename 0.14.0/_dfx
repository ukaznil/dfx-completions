#compdef dfx

autoload -U is-at-least

_dfx() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'-V[Print version information]' \
'--version[Print version information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx_commands" \
"*::: :->dfx" \
&& ret=0
    case $state in
    (dfx)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-command-$line[1]:"
        case $line[1] in
            (beta)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__beta_commands" \
"*::: :->beta" \
&& ret=0

    case $state in
    (beta)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-beta-command-$line[1]:"
        case $line[1] in
            (project)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__beta__project_commands" \
"*::: :->project" \
&& ret=0

    case $state in
    (project)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-beta-project-command-$line[1]:"
        case $line[1] in
            (import)
_arguments "${_arguments_options[@]}" \
'--prefix=[An optional prefix for canisters names to add to the project]:PREFIX: ' \
'*--network-mapping=[Networks to import canisters ids for. --network-mapping <network name in both places> --network-mapping <network name here>=<network name in project being imported> Examples: --network-mapping ic --network-mapping ic=mainnet]:NETWORK_MAPPING: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Imports all canisters found in the other project]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':location -- Path to dfx.json (local file path or url):' \
'::canister-name -- Specifies the canister name. Either this or the --all flag are required:' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(bootstrap)
_arguments "${_arguments_options[@]}" \
'--ip=[Specifies the IP address that the bootstrap server listens on. Defaults to 127.0.0.1]:IP: ' \
'--port=[Specifies the port number that the bootstrap server listens on. Defaults to 8081]:PORT: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--timeout=[Specifies the maximum number of seconds that the bootstrap server will wait for upstream requests to complete. Defaults to 30]:TIMEOUT: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(build)
_arguments "${_arguments_options[@]}" \
'--output-env-file=[Output environment variables to a file in dotenv format (without overwriting any user-defined variables, if the file already exists)]:OUTPUT_ENV_FILE: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'()--all[Builds all canisters configured in the dfx.json file]' \
'--check[Build canisters without creating them. This can be used to check that canisters build ok]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister-name -- Specifies the name of the canister to build. You must specify either a canister name or the --all option:' \
&& ret=0
;;
(cache)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__cache_commands" \
"*::: :->cache" \
&& ret=0

    case $state in
    (cache)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-cache-command-$line[1]:"
        case $line[1] in
            (delete)
_arguments "${_arguments_options[@]}" \
'--version=[]:VERSION: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(install)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(show)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(canister)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__canister_commands" \
"*::: :->canister" \
&& ret=0

    case $state in
    (canister)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-canister-command-$line[1]:"
        case $line[1] in
            (call)
_arguments "${_arguments_options[@]}" \
'(--random)--argument-file=[Specifies the file from which to read the argument to pass to the method]:ARGUMENT_FILE: ' \
'(--argument-file)--random=[Specifies the config for generating random argument]:RANDOM: ' \
'--type=[Specifies the data type for the argument when making the call using an argument]:TYPE:(idl raw)' \
'(--async)--output=[Specifies the format for displaying the method'\''s return result]:OUTPUT:(idl raw pp)' \
'--with-cycles=[Specifies the amount of cycles to send on the call. Deducted from the wallet. Requires --wallet as a flag to `dfx canister`]:WITH_CYCLES: ' \
'--candid=[Provide the .did file with which to decode the response.  Overrides value from dfx.json for project canisters]:CANDID: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--async[Specifies not to wait for the result of the call to be returned by polling the replica. Instead return a response ID]' \
'(--async)--query[Sends a query request to a canister instead of an update request]' \
'(--async --query)--update[Sends an update request to a canister. This is the default if the method is not a query method]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister-name -- Specifies the name of the canister to build. You must specify either a canister name or the --all option:' \
':method-name -- Specifies the method name to call on the canister:' \
'::argument -- Specifies the argument to pass to the method:' \
&& ret=0
;;
(create)
_arguments "${_arguments_options[@]}" \
'--with-cycles=[Specifies the initial cycle balance to deposit into the newly created canister. The specified amount needs to take the canister create fee into account. This amount is deducted from the wallet'\''s cycle balance]:WITH_CYCLES: ' \
'(--all)--specified-id=[Attempts to create the canister with this Canister ID]:PRINCIPAL: ' \
'*--controller=[Specifies the identity name or the principal of the new controller]:CONTROLLER: ' \
'-c+[Specifies the canister'\''s compute allocation. This should be a percent in the range \[0..100\]]:COMPUTE_ALLOCATION: ' \
'--compute-allocation=[Specifies the canister'\''s compute allocation. This should be a percent in the range \[0..100\]]:COMPUTE_ALLOCATION: ' \
'--memory-allocation=[Specifies how much memory the canister is allowed to use in total. This should be a value in the range \[0..12 GiB\]. A setting of 0 means the canister will have access to memory on a “best-effort” basis: It will only be charged for the memory it uses, but at any point in time may stop running if it tries to allocate more memory when there isn’t space available on the subnet]:MEMORY_ALLOCATION: ' \
'--freezing-threshold=[]:FREEZING_THRESHOLD: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Creates all canisters configured in dfx.json]' \
'--no-wallet[Performs the call with the user Identity as the Sender of messages. Bypasses the Wallet canister]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister-name -- Specifies the canister name. Either this or the --all flag are required:' \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" \
'(--no-withdrawal)--withdraw-cycles-to-canister=[Withdraw cycles from canister(s) to the specified canister/wallet before deleting]:WITHDRAW_CYCLES_TO_CANISTER: ' \
'(--withdraw-cycles-to-canister --no-withdrawal)--withdraw-cycles-to-dank-principal=[Withdraw cycles to dank with the given principal]:WITHDRAW_CYCLES_TO_DANK_PRINCIPAL: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Deletes all of the canisters configured in the dfx.json file]' \
'--no-withdrawal[Do not withdrawal cycles, just delete the canister]' \
'(--withdraw-cycles-to-canister --no-withdrawal)--withdraw-cycles-to-dank[Withdraw cycles to dank with the current principal]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the name of the canister to delete. You must specify either a canister name/id or the --all flag:' \
&& ret=0
;;
(deposit-cycles)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Deposit cycles to all of the canisters configured in the dfx.json file]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':cycles -- Specifies the amount of cycles to send on the call. Deducted from the wallet:' \
'::canister -- Specifies the name or id of the canister to receive the cycles deposit. You must specify either a canister name/id or the --all option:' \
&& ret=0
;;
(id)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister -- Specifies the name of the canister:' \
&& ret=0
;;
(info)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister -- Specifies the name or id of the canister to get its canister information:' \
&& ret=0
;;
(install)
_arguments "${_arguments_options[@]}" \
'-m+[Specifies the type of deployment. You can set the canister deployment modes to install, reinstall, or upgrade. If auto is selected, either install or upgrade will be used depending on if the canister has already been installed]:MODE:(install reinstall upgrade auto)' \
'--mode=[Specifies the type of deployment. You can set the canister deployment modes to install, reinstall, or upgrade. If auto is selected, either install or upgrade will be used depending on if the canister has already been installed]:MODE:(install reinstall upgrade auto)' \
'--argument=[Specifies the argument to pass to the method]:ARGUMENT: ' \
'--argument-type=[Specifies the data type for the argument when making the call using an argument]:ARGUMENT_TYPE:(idl raw)' \
'(--all)--wasm=[Specifies a particular WASM file to install, bypassing the dfx.json project settings]:WASM: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Deploys all canisters configured in the project dfx.json files]' \
'--async-call[Specifies not to wait for the result of the call to be returned by polling the replica. Instead return a response ID]' \
'--upgrade-unchanged[Upgrade the canister even if the .wasm did not change]' \
'-y[Skips yes/no checks by answering '\''yes'\''. Such checks usually result in data loss, so this is not recommended outside of CI]' \
'--yes[Skips yes/no checks by answering '\''yes'\''. Such checks usually result in data loss, so this is not recommended outside of CI]' \
'--no-asset-upgrade[Skips upgrading the asset canister, to only install the assets themselves]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the canister to deploy. You must specify either canister name/id or the --all option:' \
'::output-env-file -- Output environment variables to a file in dotenv format (without overwriting any user-defined variables, if the file already exists):' \
&& ret=0
;;
(metadata)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister-name -- Specifies the name of the canister to call:' \
':metadata-name -- Specifies the name of the metadata to retrieve:' \
&& ret=0
;;
(request-status)
_arguments "${_arguments_options[@]}" \
'--output=[Specifies the format for displaying the method'\''s return result]:OUTPUT:(idl raw pp)' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':request-id -- Specifies the request identifier. The request identifier is an hexadecimal string starting with 0x:' \
':canister -- Specifies the name or id of the canister onto which the request was made. If the request was made to the Management canister, specify the id of the canister it is updating/querying. If the call was proxied by the wallet, i.e. a `dfx canister call --async --wallet=<ID>` flag, specify the wallet canister id:' \
&& ret=0
;;
(send)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--status[Send the signed request-status call in the message]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':file-name -- Specifies the file name of the message:' \
&& ret=0
;;
(sign)
_arguments "${_arguments_options[@]}" \
'(--random)--argument-file=[Specifies the file from which to read the argument to pass to the method]:ARGUMENT_FILE: ' \
'()--random=[Specifies the config for generating random argument]:RANDOM: ' \
'--type=[Specifies the data type for the argument when making the call using an argument]:TYPE:(idl raw)' \
'--expire-after=[Specifies how long the message will be valid in seconds, default to be 300s (5 minutes)]:EXPIRE_AFTER: ' \
'--file=[Specifies the output file name]:FILE: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--query[Sends a query request to a canister]' \
'(--query)--update[Sends an update request to a canister. This is the default if the method is not a query method]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister-name -- Specifies the name of the canister to call:' \
':method-name -- Specifies the method name to call on the canister:' \
'::argument -- Specifies the argument to pass to the method:' \
&& ret=0
;;
(start)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Starts all of the canisters configured in the dfx.json file]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the name or id of the canister to start. You must specify either a canister name/id or the --all flag:' \
&& ret=0
;;
(status)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Returns status information for all of the canisters configured in the dfx.json file]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the name of the canister to return information for. You must specify either a canister name or the --all flag:' \
&& ret=0
;;
(stop)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Stops all of the canisters configured in the dfx.json file]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the name or id of the canister to stop. You must specify either a canister name/id or the --all option:' \
&& ret=0
;;
(uninstall-code)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Uninstalls all of the canisters configured in the dfx.json file]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the name or id of the canister to uinstall. You must specify either a canister name/id or the --all option:' \
&& ret=0
;;
(update-settings)
_arguments "${_arguments_options[@]}" \
'*--set-controller=[Specifies the identity name or the principal of the new controller. Can be specified more than once, indicating the canister will have multiple controllers. If any controllers are set with this parameter, any other controllers will be removed]:SET_CONTROLLER: ' \
'(--set-controller)*--add-controller=[Add a principal to the list of controllers of the canister]:ADD_CONTROLLER: ' \
'(--set-controller)*--remove-controller=[Removes a principal from the list of controllers of the canister]:REMOVE_CONTROLLER: ' \
'-c+[Specifies the canister'\''s compute allocation. This should be a percent in the range \[0..100\]]:COMPUTE_ALLOCATION: ' \
'--compute-allocation=[Specifies the canister'\''s compute allocation. This should be a percent in the range \[0..100\]]:COMPUTE_ALLOCATION: ' \
'--memory-allocation=[Specifies how much memory the canister is allowed to use in total. This should be a value in the range \[0..12 GiB\]. A setting of 0 means the canister will have access to memory on a “best-effort” basis: It will only be charged for the memory it uses, but at any point in time may stop running if it tries to allocate more memory when there isn’t space available on the subnet]:MEMORY_ALLOCATION: ' \
'--freezing-threshold=[Sets the freezing_threshold in SECONDS. A canister is considered frozen whenever the IC estimates that the canister would be depleted of cycles before freezing_threshold seconds pass, given the canister'\''s current size and the IC'\''s current cost for storage. A frozen canister rejects any calls made to it]:FREEZING_THRESHOLD: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Updates the settings of all canisters configured in the project dfx.json files]' \
'--confirm-very-long-freezing-threshold[Freezing thresholds above ~1.5 years require this flag as confirmation]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the canister name or id to update. You must specify either canister name/id or the --all option:' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(deploy)
_arguments "${_arguments_options[@]}" \
'--argument=[Specifies the argument to pass to the method]:ARGUMENT: ' \
'--argument-type=[Specifies the data type for the argument when making the call using an argument]:ARGUMENT_TYPE:(idl raw)' \
'-m+[Force the type of deployment to be reinstall, which overwrites the module. In other words, this erases all data in the canister. By default, upgrade will be chosen automatically if the module already exists, or install if it does not]:MODE:(reinstall)' \
'--mode=[Force the type of deployment to be reinstall, which overwrites the module. In other words, this erases all data in the canister. By default, upgrade will be chosen automatically if the module already exists, or install if it does not]:MODE:(reinstall)' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--with-cycles=[Specifies the initial cycle balance to deposit into the newly created canister. The specified amount needs to take the canister create fee into account. This amount is deducted from the wallet'\''s cycle balance]:WITH_CYCLES: ' \
'--specified-id=[Attempts to create the canister with this Canister ID]:PRINCIPAL: ' \
'--wallet=[Specify a wallet canister id to perform the call. If none specified, defaults to use the selected Identity'\''s wallet canister]:WALLET: ' \
'--output-env-file=[Output environment variables to a file in dotenv format (without overwriting any user-defined variables, if the file already exists)]:OUTPUT_ENV_FILE: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--upgrade-unchanged[Upgrade the canister even if the .wasm did not change]' \
'(--wallet)--no-wallet[Performs the create call with the user Identity as the Sender of messages. Bypasses the Wallet canister]' \
'-y[Skips yes/no checks by answering '\''yes'\''. Such checks usually result in data loss, so this is not recommended outside of CI]' \
'--yes[Skips yes/no checks by answering '\''yes'\''. Such checks usually result in data loss, so this is not recommended outside of CI]' \
'--no-asset-upgrade[Skips upgrading the asset canister, to only install the assets themselves]' \
'(--compute-evidence)--by-proposal[Prepare (upload) assets for later commit by proposal]' \
'(--by-proposal)--compute-evidence[Compute evidence and compare it against expected evidence]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister-name -- Specifies the name of the canister you want to deploy. If you don’t specify a canister name, all canisters defined in the dfx.json file are deployed:' \
&& ret=0
;;
(diagnose)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(fix)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(generate)
_arguments "${_arguments_options[@]}" \
'--network=[]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister-name -- Specifies the name of the canister to generate type information for. If you do not specify a canister name, generates types for all canisters:' \
&& ret=0
;;
(identity)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__identity_commands" \
"*::: :->identity" \
&& ret=0

    case $state in
    (identity)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-identity-command-$line[1]:"
        case $line[1] in
            (deploy-wallet)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister-id -- The ID of the canister where the wallet WASM will be deployed:' \
&& ret=0
;;
(export)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':exported-identity -- The identity to export:' \
&& ret=0
;;
(get-wallet)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(import)
_arguments "${_arguments_options[@]}" \
'()--seed-file=[The path to a file with your seed phrase]:SEED_FILE: ' \
'(--disable-encryption)--storage-mode=[How your private keys are stored. By default, if keyring/keychain is available, keys are stored there. Otherwise, a password-protected file is used as fallback. Mode '\''plaintext'\'' is not safe, but convenient for use in CI]:STORAGE_MODE:(keyring password-protected plaintext)' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--disable-encryption[DEPRECATED: Please use --storage-mode=plaintext instead]' \
'--force[If the identity already exists, remove and re-import it]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':new-identity -- The identity to create:' \
'::pem-file -- The PEM file to import:' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(new)
_arguments "${_arguments_options[@]}" \
'--hsm-pkcs11-lib-path=[The file path to the opensc-pkcs11 library e.g. "/usr/local/lib/opensc-pkcs11.so"]:HSM_PKCS11_LIB_PATH: ' \
'--hsm-key-id=[A sequence of pairs of hex digits]:HSM_KEY_ID: ' \
'(--disable-encryption)--storage-mode=[How your private keys are stored. By default, if keyring/keychain is available, keys are stored there. Otherwise, a password-protected file is used as fallback. Mode '\''plaintext'\'' is not safe, but convenient for use in CI]:STORAGE_MODE:(keyring password-protected plaintext)' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--disable-encryption[DEPRECATED: Please use --storage-mode=plaintext instead]' \
'--force[If the identity already exists, remove and re-create it]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':new-identity -- The name of the identity to create:' \
&& ret=0
;;
(get-principal)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--drop-wallets[Required if the identity has wallets configured so that users do not accidentally lose access to wallets]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':removed-identity -- The identity to remove:' \
&& ret=0
;;
(rename)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':from -- The current name of the identity:' \
':to -- The new name of the identity:' \
&& ret=0
;;
(set-wallet)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--force[Skip verification that the ID points to a correct wallet canister. Only useful for the local network]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister-name -- The Canister ID of the wallet to associate with this identity:' \
&& ret=0
;;
(use)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':new-identity -- The identity to use:' \
&& ret=0
;;
(whoami)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(info)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__info_commands" \
"*::: :->info" \
&& ret=0

    case $state in
    (info)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-info-command-$line[1]:"
        case $line[1] in
            (replica-port)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(replica-rev)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(webserver-port)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(networks-json-path)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(_language-service)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--force-tty[Forces the language server to start even when run from a terminal]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the canister name. If you don'\''t specify this argument, all canisters are processed:' \
&& ret=0
;;
(ledger)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__ledger_commands" \
"*::: :->ledger" \
&& ret=0

    case $state in
    (ledger)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-ledger-command-$line[1]:"
        case $line[1] in
            (account-id)
_arguments "${_arguments_options[@]}" \
'--of-principal=[Principal controlling the account]:PRINCIPAL: ' \
'--of-canister=[Alias or principal of the canister controlling the account]:ALIAS: ' \
'--subaccount=[Subaccount identifier (64 character long hex string)]:SUBACCOUNT: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(balance)
_arguments "${_arguments_options[@]}" \
'()--subaccount=[Subaccount of the selected identity to get the balance of]:SUBACCOUNT: ' \
'--ledger-canister-id=[Canister ID of the ledger canister]:LEDGER_CANISTER_ID: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::of -- Specifies an AccountIdentifier to get the balance of:' \
&& ret=0
;;
(create-canister)
_arguments "${_arguments_options[@]}" \
'--from-subaccount=[Subaccount to withdraw from]:FROM_SUBACCOUNT: ' \
'--amount=[ICP to mint into cycles and deposit into destination canister Can be specified as a Decimal with the fractional portion up to 8 decimal places i.e. 100.012]:AMOUNT: ' \
'(--amount)--icp=[Specify ICP as a whole number, helpful for use in conjunction with `--e8s`]:ICP: ' \
'(--amount)--e8s=[Specify e8s as a whole number, helpful for use in conjunction with `--icp`]:E8S: ' \
'--fee=[Transaction fee, default is 10000 e8s]:FEE: ' \
'--max-fee=[Max fee, default is 10000 e8s]:MAX_FEE: ' \
'--created-at-time=[Transaction timestamp, in nanoseconds, for use in controlling transaction-deduplication, default is system-time. // https://internetcomputer.org/docs/current/developer-docs/integrations/icrc-1/#transaction-deduplication-]:CREATED_AT_TIME: ' \
'--subnet-type=[Specify the optional subnet type to create the canister on. If no subnet type is provided, the canister will be created on a random default application subnet]:SUBNET_TYPE: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':controller -- Specify the controller of the new canister:' \
&& ret=0
;;
(fabricate-cycles)
_arguments "${_arguments_options[@]}" \
'(--t --amount --icp --e8s)--cycles=[Specifies the amount of cycles to fabricate]:CYCLES: ' \
'(--cycles --icp --e8s --t)--amount=[ICP to mint into cycles and deposit into destination canister Can be specified as a Decimal with the fractional portion up to 8 decimal places i.e. 100.012]:AMOUNT: ' \
'(--amount --cycles --t)--icp=[Specify ICP as a whole number, helpful for use in conjunction with `--e8s`]:ICP: ' \
'(--amount --cycles --t)--e8s=[Specify e8s as a whole number, helpful for use in conjunction with `--icp`]:E8S: ' \
'(--amount)--t=[Specifies the amount of trillion cycles to fabricate]:T: ' \
'--canister=[Specifies the name or id of the canister to receive the cycles deposit. You must specify either a canister name/id or the --all option]:CANISTER: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Deposit cycles to all of the canisters configured in the dfx.json file]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(notify)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__ledger__notify_commands" \
"*::: :->notify" \
&& ret=0

    case $state in
    (notify)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-ledger-notify-command-$line[1]:"
        case $line[1] in
            (create-canister)
_arguments "${_arguments_options[@]}" \
'--subnet-type=[Specify the optional subnet type to create the canister on. If no subnet type is provided, the canister will be created on a random default application subnet]:SUBNET_TYPE: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':block-height -- BlockHeight at which the send transation was recorded:' \
':controller -- The controller of the created canister:' \
&& ret=0
;;
(top-up)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':block-height -- BlockHeight at which the send transation was recorded:' \
':canister -- The principal of the canister to top up:' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(show-subnet-types)
_arguments "${_arguments_options[@]}" \
'--cycles-minting-canister-id=[Canister ID of the cycles minting canister]:CYCLES_MINTING_CANISTER_ID: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(top-up)
_arguments "${_arguments_options[@]}" \
'--from-subaccount=[Subaccount to withdraw from]:FROM_SUBACCOUNT: ' \
'--amount=[ICP to mint into cycles and deposit into destination canister Can be specified as a Decimal with the fractional portion up to 8 decimal places i.e. 100.012]:AMOUNT: ' \
'(--amount)--icp=[Specify ICP as a whole number, helpful for use in conjunction with `--e8s`]:ICP: ' \
'(--amount)--e8s=[Specify e8s as a whole number, helpful for use in conjunction with `--icp`]:E8S: ' \
'--fee=[Transaction fee, default is 10000 e8s]:FEE: ' \
'--max-fee=[Max fee, default is 10000 e8s]:MAX_FEE: ' \
'--created-at-time=[Transaction timestamp, in nanoseconds, for use in controlling transaction-deduplication, default is system-time. // https://internetcomputer.org/docs/current/developer-docs/integrations/icrc-1/#transaction-deduplication-]:CREATED_AT_TIME: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':canister -- Specify the canister id to top up:' \
&& ret=0
;;
(transfer)
_arguments "${_arguments_options[@]}" \
'--from-subaccount=[Subaccount to transfer from]:FROM_SUBACCOUNT: ' \
'--amount=[ICPs to transfer to the destination AccountIdentifier Can be specified as a Decimal with the fractional portion up to 8 decimal places i.e. 100.012]:AMOUNT: ' \
'(--amount)--icp=[Specify ICP as a whole number, helpful for use in conjunction with `--e8s`]:ICP: ' \
'(--amount)--e8s=[Specify e8s as a whole number, helpful for use in conjunction with `--icp`]:E8S: ' \
'--memo=[Specify a numeric memo for this transaction]:MEMO: ' \
'--fee=[Transaction fee, default is 10000 e8s]:FEE: ' \
'--ledger-canister-id=[Canister ID of the ledger canister]:LEDGER_CANISTER_ID: ' \
'--created-at-time=[Transaction timestamp, in nanoseconds, for use in controlling transaction-deduplication, default is system-time. // https://internetcomputer.org/docs/current/developer-docs/integrations/icrc-1/#transaction-deduplication-]:CREATED_AT_TIME: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':to -- AccountIdentifier of transfer destination:' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(new)
_arguments "${_arguments_options[@]}" \
'--type=[Choose the type of canister in the starter project. Default to be motoko]:TYPE:(motoko rust)' \
'--agent-version=[Overrides which version of the JavaScript Agent to install. By default, will contact NPM to decide]:AGENT_VERSION: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--dry-run[Provides a preview the directories and files to be created without adding them to the file system]' \
'--frontend[Installs the frontend code example for the default canister. This defaults to true if Node is installed, or false if it isn'\''t]' \
'(--frontend)--no-frontend[Skip installing the frontend code example]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':project-name -- Specifies the name of the project to create:' \
&& ret=0
;;
(nns)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__nns_commands" \
"*::: :->nns" \
&& ret=0

    case $state in
    (nns)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-nns-command-$line[1]:"
        case $line[1] in
            (import)
_arguments "${_arguments_options[@]}" \
'*--network-mapping=[Networks to import canisters ids for. --network-mapping <network name in both places> --network-mapping <network name here>=<network name in project being imported> Examples: --network-mapping ic --network-mapping ic=mainnet]:NETWORK_MAPPING: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(install)
_arguments "${_arguments_options[@]}" \
'*--ledger-accounts=[Initialize ledger canister with these test accounts]:LEDGER_ACCOUNTS: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(ping)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--wait-healthy[Repeatedly ping until the replica is healthy]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::network -- The provider to use. A valid URL (starting with `http\:` or `https\:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http\://localhost\:12345/" is a valid network name:' \
&& ret=0
;;
(pull)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister-name -- Specifies the name of the canister you want to pull. If you don’t specify a canister name, all pull type canisters defined in the dfx.json file are pulled:' \
&& ret=0
;;
(quickstart)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(remote)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__remote_commands" \
"*::: :->remote" \
&& ret=0

    case $state in
    (remote)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-remote-command-$line[1]:"
        case $line[1] in
            (generate-binding)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--all[Builds bindings for all canisters]' \
'--overwrite[Overwrite main file if it already exists]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::canister -- Specifies the name of the canister to generate bindings for. You must specify either canister name/id or the --all option. Generates bindings into <canister-name>.main from <canister-name>.remote.candid:' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(replica)
_arguments "${_arguments_options[@]}" \
'--port=[Specifies the port the local replica should listen to]:PORT: ' \
'(--emulator)*--bitcoin-node=[Address of bitcoind node.  Implies --enable-bitcoin]:BITCOIN_NODE: ' \
'(--emulator)--artificial-delay=[The delay (in milliseconds) an update call should take. Lower values may be expedient in CI]:ARTIFICIAL_DELAY: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--emulator[Runs a dedicated emulator instead of the replica]' \
'(--emulator)--enable-bitcoin[enable bitcoin integration]' \
'(--emulator)--enable-canister-http[enable canister http requests]' \
'--force[Start even if the network config was modified]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(schema)
_arguments "${_arguments_options[@]}" \
'--for=[]:FOR: ' \
'--outfile=[Outputs the schema to the specified file]:OUTFILE: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(sns)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__sns_commands" \
"*::: :->sns" \
&& ret=0

    case $state in
    (sns)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-sns-command-$line[1]:"
        case $line[1] in
            (config)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__sns__config_commands" \
"*::: :->config" \
&& ret=0

    case $state in
    (config)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-sns-config-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(deploy)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(import)
_arguments "${_arguments_options[@]}" \
'*--network-mapping=[Networks to import canisters ids for. --network-mapping <network name in both places> --network-mapping <network name here>=<network name in project being imported> Examples: --network-mapping ic --network-mapping ic=mainnet]:NETWORK_MAPPING: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(download)
_arguments "${_arguments_options[@]}" \
'--ic-commit=[IC commit of SNS canister WASMs to download]:IC_COMMIT: ' \
'--wasms-dir=[Path to store downloaded SNS canister WASMs]:WASMS_DIR: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(start)
_arguments "${_arguments_options[@]}" \
'--host=[Specifies the host name and port number to bind the frontend to]:HOST: ' \
'(--emulator)*--bitcoin-node=[Address of bitcoind node.  Implies --enable-bitcoin]:BITCOIN_NODE: ' \
'(--emulator)--artificial-delay=[The delay (in milliseconds) an update call should take. Lower values may be expedient in CI]:ARTIFICIAL_DELAY: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--background[Exits the dfx leaving the replica running. Will wait until the replica replies before exiting]' \
'--clean[Cleans the state of the current project]' \
'--emulator[Runs a dedicated emulator instead of the replica]' \
'(--emulator)--enable-bitcoin[enable bitcoin integration]' \
'(--emulator)--enable-canister-http[enable canister http requests]' \
'--force[Start even if the network config was modified]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(stop)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(toolchain)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__toolchain_commands" \
"*::: :->toolchain" \
&& ret=0

    case $state in
    (toolchain)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-toolchain-command-$line[1]:"
        case $line[1] in
            (install)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::toolchains -- Toolchain name, such as '\''0.6.22'\'', '\''0.6'\'', '\''latest'\'':' \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::toolchains -- Toolchain name, such as '\''0.6.22'\'', '\''0.6'\'', '\''latest'\'':' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(default)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'::toolchain -- Toolchain name, such as '\''0.6.22'\'', '\''0.6'\'', '\''latest'\'':' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(upgrade)
_arguments "${_arguments_options[@]}" \
'--current-version=[Current Version]:CURRENT_VERSION: ' \
'--release-root=[]:RELEASE_ROOT: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(wallet)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
":: :_dfx__wallet_commands" \
"*::: :->wallet" \
&& ret=0

    case $state in
    (wallet)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:dfx-wallet-command-$line[1]:"
        case $line[1] in
            (addresses)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(add-controller)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':controller -- Principal of the controller to add:' \
&& ret=0
;;
(authorize)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':custodian -- Principal of the custodian to authorize:' \
&& ret=0
;;
(balance)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'--precise[Get balance raw value (without upscaling to trillions of cycles)]' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(controllers)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(custodians)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(deauthorize)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':custodian -- Principal of the custodian to deauthorize:' \
&& ret=0
;;
(name)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(redeem-faucet-coupon)
_arguments "${_arguments_options[@]}" \
'--faucet=[Alternative faucet address. If not set, this uses the DFINITY faucet]:FAUCET: ' \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':coupon-code -- The coupon code to redeem at the faucet:' \
&& ret=0
;;
(remove-controller)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':controller -- Principal of the controller to remove:' \
&& ret=0
;;
(send)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':destination -- Canister ID of the destination cycles wallet:' \
':amount -- Specifies the amount of cycles to send. Deducted from the wallet:' \
&& ret=0
;;
(set-name)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
':name -- Name of the wallet:' \
&& ret=0
;;
(upgrade)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" \
'--network=[Override the compute network to connect to. By default, the local network is used. A valid URL (starting with `http:` or `https:`) can be used here, and a special ephemeral network will be created specifically for this request. E.g. "http://localhost:12345/" is a valid network name]:NETWORK: ' \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
;;
(help)
_arguments "${_arguments_options[@]}" \
'--log=[The logging mode to use. You can log to stderr, a file, or both]:LOGMODE:(stderr tee file)' \
'--logfile=[The file to log to, if logging to a file (see --logmode)]:LOGFILE: ' \
'--identity=[The user identity to run this command as. It contains your principal as well as some things DFX associates with it like the wallet]:IDENTITY: ' \
'--provisional-create-canister-effective-canister-id=[The effective canister id for provisional canister creation must be a canister id in the canister ranges of the subnet on which new canisters should be created]:PRINCIPAL: ' \
'*-v[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*--verbose[Displays detailed information about operations. -vv will generate a very large number of messages and can affect performance]' \
'*-q[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*--quiet[Suppresses informational messages. -qq limits to errors only; -qqqq disables them all]' \
'*::subcommand -- The subcommand whose help message to display:' \
&& ret=0
;;
        esac
    ;;
esac
}

(( $+functions[_dfx_commands] )) ||
_dfx_commands() {
    local commands; commands=(
'beta:Beta commands' \
'bootstrap:Starts the bootstrap server' \
'build:Builds all or specific canisters from the code in your project. By default, all canisters are built' \
'cache:Manages the dfx version cache' \
'canister:Manages canisters deployed on a network replica' \
'deploy:Deploys all or a specific canister from the code in your project. By default, all canisters are deployed' \
'diagnose:Detects known problems in the current environment caused by upgrading DFX, and suggests commands to fix them. These commands can be batch-run automatically via `dfx fix`' \
'fix:Applies one-time fixes for known problems in the current environment caused by upgrading DFX. Makes no changes that would not have been suggested by `dfx diagnose`' \
'generate:Generate type declarations for canisters from the code in your project' \
'identity:Manages identities used to communicate with the Internet Computer network. Setting an identity enables you to test user-based access controls' \
'info:Get information about the replica shipped with dfx, path to networks.json, and network ports of running replica' \
'_language-service:Starts the Motoko IDE Language Server. This is meant to be run by editor plugins not the end-user' \
'ledger:Ledger commands' \
'new:Creates a new project' \
'nns:Options for `dfx nns` and its subcommands' \
'ping:Pings an Internet Computer network and returns its status' \
'pull:Pull canisters upon which the project depends' \
'quickstart:Use the `dfx quickstart` command to perform initial one time setup for your identity and/or wallet. This command can be run anytime to repeat the setup process or to be used as an informational command, printing information about your ICP balance, current ICP to XDR conversion rate, and more' \
'remote:Commands used to work with remote canisters' \
'replica:Starts a local Internet Computer replica' \
'schema:Prints the schema for dfx.json' \
'sns:Options for `dfx sns`' \
'start:Starts the local replica and a web server for the current project' \
'stop:Stops the local network replica' \
'toolchain:Manage the dfx toolchains' \
'upgrade:Upgrade DFX' \
'wallet:Helper commands to manage the user'\''s cycles wallet' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx commands' commands "$@"
}
(( $+functions[_dfx___language-service_commands] )) ||
_dfx___language-service_commands() {
    local commands; commands=()
    _describe -t commands 'dfx _language-service commands' commands "$@"
}
(( $+functions[_dfx__ledger__account-id_commands] )) ||
_dfx__ledger__account-id_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger account-id commands' commands "$@"
}
(( $+functions[_dfx__wallet__add-controller_commands] )) ||
_dfx__wallet__add-controller_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet add-controller commands' commands "$@"
}
(( $+functions[_dfx__wallet__addresses_commands] )) ||
_dfx__wallet__addresses_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet addresses commands' commands "$@"
}
(( $+functions[_dfx__wallet__authorize_commands] )) ||
_dfx__wallet__authorize_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet authorize commands' commands "$@"
}
(( $+functions[_dfx__ledger__balance_commands] )) ||
_dfx__ledger__balance_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger balance commands' commands "$@"
}
(( $+functions[_dfx__wallet__balance_commands] )) ||
_dfx__wallet__balance_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet balance commands' commands "$@"
}
(( $+functions[_dfx__beta_commands] )) ||
_dfx__beta_commands() {
    local commands; commands=(
'project:Project commands' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx beta commands' commands "$@"
}
(( $+functions[_dfx__bootstrap_commands] )) ||
_dfx__bootstrap_commands() {
    local commands; commands=()
    _describe -t commands 'dfx bootstrap commands' commands "$@"
}
(( $+functions[_dfx__build_commands] )) ||
_dfx__build_commands() {
    local commands; commands=()
    _describe -t commands 'dfx build commands' commands "$@"
}
(( $+functions[_dfx__cache_commands] )) ||
_dfx__cache_commands() {
    local commands; commands=(
'delete:Deletes a specific versioned cache of dfx' \
'install:Forces unpacking the cache from this dfx version' \
'list:Lists installed and used version' \
'show:Shows the path of the cache used by this version' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx cache commands' commands "$@"
}
(( $+functions[_dfx__canister__call_commands] )) ||
_dfx__canister__call_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister call commands' commands "$@"
}
(( $+functions[_dfx__canister_commands] )) ||
_dfx__canister_commands() {
    local commands; commands=(
'call:Calls a method on a deployed canister' \
'create:Creates an empty canister and associates the assigned Canister ID to the canister name' \
'delete:Deletes a currently stopped canister' \
'deposit-cycles:Deposit cycles into the specified canister' \
'id:Prints the identifier of a canister' \
'info:Get the hash of a canister’s WASM module and its current controllers' \
'install:Installs compiled code in a canister' \
'metadata:Displays metadata in a canister' \
'request-status:Requests the status of a call from a canister' \
'send:Send a previously-signed message' \
'sign:Sign a canister call and generate message file' \
'start:Starts a stopped canister' \
'status:Returns the current status of a canister: Running, Stopping, or Stopped. Also carries information like balance, current settings, memory used and everything returned by '\''info'\''' \
'stop:Stops a currently running canister' \
'uninstall-code:Uninstalls a canister, removing its code and state. Does not delete the canister' \
'update-settings:Update one or more of a canister'\''s settings (i.e its controller, compute allocation, or memory allocation.)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx canister commands' commands "$@"
}
(( $+functions[_dfx__sns__config_commands] )) ||
_dfx__sns__config_commands() {
    local commands; commands=(
'create:Command line options for creating an SNS configuration' \
'validate:Command line options for validating an SNS configuration' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx sns config commands' commands "$@"
}
(( $+functions[_dfx__wallet__controllers_commands] )) ||
_dfx__wallet__controllers_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet controllers commands' commands "$@"
}
(( $+functions[_dfx__canister__create_commands] )) ||
_dfx__canister__create_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister create commands' commands "$@"
}
(( $+functions[_dfx__sns__config__create_commands] )) ||
_dfx__sns__config__create_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns config create commands' commands "$@"
}
(( $+functions[_dfx__ledger__create-canister_commands] )) ||
_dfx__ledger__create-canister_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger create-canister commands' commands "$@"
}
(( $+functions[_dfx__ledger__notify__create-canister_commands] )) ||
_dfx__ledger__notify__create-canister_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger notify create-canister commands' commands "$@"
}
(( $+functions[_dfx__wallet__custodians_commands] )) ||
_dfx__wallet__custodians_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet custodians commands' commands "$@"
}
(( $+functions[_dfx__wallet__deauthorize_commands] )) ||
_dfx__wallet__deauthorize_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet deauthorize commands' commands "$@"
}
(( $+functions[_dfx__toolchain__default_commands] )) ||
_dfx__toolchain__default_commands() {
    local commands; commands=()
    _describe -t commands 'dfx toolchain default commands' commands "$@"
}
(( $+functions[_dfx__cache__delete_commands] )) ||
_dfx__cache__delete_commands() {
    local commands; commands=()
    _describe -t commands 'dfx cache delete commands' commands "$@"
}
(( $+functions[_dfx__canister__delete_commands] )) ||
_dfx__canister__delete_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister delete commands' commands "$@"
}
(( $+functions[_dfx__deploy_commands] )) ||
_dfx__deploy_commands() {
    local commands; commands=()
    _describe -t commands 'dfx deploy commands' commands "$@"
}
(( $+functions[_dfx__sns__deploy_commands] )) ||
_dfx__sns__deploy_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns deploy commands' commands "$@"
}
(( $+functions[_dfx__identity__deploy-wallet_commands] )) ||
_dfx__identity__deploy-wallet_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity deploy-wallet commands' commands "$@"
}
(( $+functions[_dfx__canister__deposit-cycles_commands] )) ||
_dfx__canister__deposit-cycles_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister deposit-cycles commands' commands "$@"
}
(( $+functions[_dfx__diagnose_commands] )) ||
_dfx__diagnose_commands() {
    local commands; commands=()
    _describe -t commands 'dfx diagnose commands' commands "$@"
}
(( $+functions[_dfx__sns__download_commands] )) ||
_dfx__sns__download_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns download commands' commands "$@"
}
(( $+functions[_dfx__identity__export_commands] )) ||
_dfx__identity__export_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity export commands' commands "$@"
}
(( $+functions[_dfx__ledger__fabricate-cycles_commands] )) ||
_dfx__ledger__fabricate-cycles_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger fabricate-cycles commands' commands "$@"
}
(( $+functions[_dfx__fix_commands] )) ||
_dfx__fix_commands() {
    local commands; commands=()
    _describe -t commands 'dfx fix commands' commands "$@"
}
(( $+functions[_dfx__generate_commands] )) ||
_dfx__generate_commands() {
    local commands; commands=()
    _describe -t commands 'dfx generate commands' commands "$@"
}
(( $+functions[_dfx__remote__generate-binding_commands] )) ||
_dfx__remote__generate-binding_commands() {
    local commands; commands=()
    _describe -t commands 'dfx remote generate-binding commands' commands "$@"
}
(( $+functions[_dfx__identity__get-principal_commands] )) ||
_dfx__identity__get-principal_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity get-principal commands' commands "$@"
}
(( $+functions[_dfx__identity__get-wallet_commands] )) ||
_dfx__identity__get-wallet_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity get-wallet commands' commands "$@"
}
(( $+functions[_dfx__beta__help_commands] )) ||
_dfx__beta__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx beta help commands' commands "$@"
}
(( $+functions[_dfx__beta__project__help_commands] )) ||
_dfx__beta__project__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx beta project help commands' commands "$@"
}
(( $+functions[_dfx__cache__help_commands] )) ||
_dfx__cache__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx cache help commands' commands "$@"
}
(( $+functions[_dfx__canister__help_commands] )) ||
_dfx__canister__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister help commands' commands "$@"
}
(( $+functions[_dfx__help_commands] )) ||
_dfx__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx help commands' commands "$@"
}
(( $+functions[_dfx__identity__help_commands] )) ||
_dfx__identity__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity help commands' commands "$@"
}
(( $+functions[_dfx__info__help_commands] )) ||
_dfx__info__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx info help commands' commands "$@"
}
(( $+functions[_dfx__ledger__help_commands] )) ||
_dfx__ledger__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger help commands' commands "$@"
}
(( $+functions[_dfx__ledger__notify__help_commands] )) ||
_dfx__ledger__notify__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger notify help commands' commands "$@"
}
(( $+functions[_dfx__nns__help_commands] )) ||
_dfx__nns__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx nns help commands' commands "$@"
}
(( $+functions[_dfx__remote__help_commands] )) ||
_dfx__remote__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx remote help commands' commands "$@"
}
(( $+functions[_dfx__sns__config__help_commands] )) ||
_dfx__sns__config__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns config help commands' commands "$@"
}
(( $+functions[_dfx__sns__help_commands] )) ||
_dfx__sns__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns help commands' commands "$@"
}
(( $+functions[_dfx__toolchain__help_commands] )) ||
_dfx__toolchain__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx toolchain help commands' commands "$@"
}
(( $+functions[_dfx__wallet__help_commands] )) ||
_dfx__wallet__help_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet help commands' commands "$@"
}
(( $+functions[_dfx__canister__id_commands] )) ||
_dfx__canister__id_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister id commands' commands "$@"
}
(( $+functions[_dfx__identity_commands] )) ||
_dfx__identity_commands() {
    local commands; commands=(
'deploy-wallet:Installs the wallet WASM to the provided canister id' \
'export:Prints the decrypted PEM file for the identity' \
'get-wallet:Gets the canister ID for the wallet associated with your identity on a network' \
'import:Creates a new identity from a PEM file' \
'list:Lists existing identities' \
'new:Creates a new identity' \
'get-principal:Shows the textual representation of the Principal associated with the current identity' \
'remove:Removes an existing identity' \
'rename:Renames an existing identity' \
'set-wallet:Sets the wallet canister ID to use for your identity on a network' \
'use:Specifies the identity to use' \
'whoami:Shows the name of the current identity' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx identity commands' commands "$@"
}
(( $+functions[_dfx__beta__project__import_commands] )) ||
_dfx__beta__project__import_commands() {
    local commands; commands=()
    _describe -t commands 'dfx beta project import commands' commands "$@"
}
(( $+functions[_dfx__identity__import_commands] )) ||
_dfx__identity__import_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity import commands' commands "$@"
}
(( $+functions[_dfx__nns__import_commands] )) ||
_dfx__nns__import_commands() {
    local commands; commands=()
    _describe -t commands 'dfx nns import commands' commands "$@"
}
(( $+functions[_dfx__sns__import_commands] )) ||
_dfx__sns__import_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns import commands' commands "$@"
}
(( $+functions[_dfx__canister__info_commands] )) ||
_dfx__canister__info_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister info commands' commands "$@"
}
(( $+functions[_dfx__info_commands] )) ||
_dfx__info_commands() {
    local commands; commands=(
'replica-port:Show the port of the local replica' \
'replica-rev:Show the revision of the replica shipped with this dfx binary' \
'webserver-port:Show the port of the webserver' \
'networks-json-path:Show the path to network configuration file' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx info commands' commands "$@"
}
(( $+functions[_dfx__cache__install_commands] )) ||
_dfx__cache__install_commands() {
    local commands; commands=()
    _describe -t commands 'dfx cache install commands' commands "$@"
}
(( $+functions[_dfx__canister__install_commands] )) ||
_dfx__canister__install_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister install commands' commands "$@"
}
(( $+functions[_dfx__nns__install_commands] )) ||
_dfx__nns__install_commands() {
    local commands; commands=()
    _describe -t commands 'dfx nns install commands' commands "$@"
}
(( $+functions[_dfx__toolchain__install_commands] )) ||
_dfx__toolchain__install_commands() {
    local commands; commands=()
    _describe -t commands 'dfx toolchain install commands' commands "$@"
}
(( $+functions[_dfx__ledger_commands] )) ||
_dfx__ledger_commands() {
    local commands; commands=(
'account-id:Prints the ledger account identifier corresponding to a principal' \
'balance:Prints the account balance of the user' \
'create-canister:Create a canister from ICP' \
'fabricate-cycles:Local development only: Fabricate cycles out of thin air and deposit them into the specified canister(s). Can specify a number of ICP/e8s (which will be converted to cycles using the current exchange rate) or a number of cycles. If no amount is specified, 10T cycles are added' \
'notify:Notify the ledger about a send transaction to the cycles minting canister. This command should only be used if `dfx ledger create-canister` or `dfx ledger top-up` successfully sent a message to the ledger, and a transaction was recorded at some block height, but for some reason the subsequent notify failed' \
'show-subnet-types:Show available subnet types in the cycles minting canister' \
'top-up:Top up a canister with cycles minted from ICP' \
'transfer:Transfer ICP from the user to the destination account identifier' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx ledger commands' commands "$@"
}
(( $+functions[_dfx__cache__list_commands] )) ||
_dfx__cache__list_commands() {
    local commands; commands=()
    _describe -t commands 'dfx cache list commands' commands "$@"
}
(( $+functions[_dfx__identity__list_commands] )) ||
_dfx__identity__list_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity list commands' commands "$@"
}
(( $+functions[_dfx__toolchain__list_commands] )) ||
_dfx__toolchain__list_commands() {
    local commands; commands=()
    _describe -t commands 'dfx toolchain list commands' commands "$@"
}
(( $+functions[_dfx__canister__metadata_commands] )) ||
_dfx__canister__metadata_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister metadata commands' commands "$@"
}
(( $+functions[_dfx__wallet__name_commands] )) ||
_dfx__wallet__name_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet name commands' commands "$@"
}
(( $+functions[_dfx__info__networks-json-path_commands] )) ||
_dfx__info__networks-json-path_commands() {
    local commands; commands=()
    _describe -t commands 'dfx info networks-json-path commands' commands "$@"
}
(( $+functions[_dfx__identity__new_commands] )) ||
_dfx__identity__new_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity new commands' commands "$@"
}
(( $+functions[_dfx__new_commands] )) ||
_dfx__new_commands() {
    local commands; commands=()
    _describe -t commands 'dfx new commands' commands "$@"
}
(( $+functions[_dfx__nns_commands] )) ||
_dfx__nns_commands() {
    local commands; commands=(
'import:Import NNS API definitions and canister IDs' \
'install:Install an NNS on the local dfx server' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx nns commands' commands "$@"
}
(( $+functions[_dfx__ledger__notify_commands] )) ||
_dfx__ledger__notify_commands() {
    local commands; commands=(
'create-canister:' \
'top-up:' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx ledger notify commands' commands "$@"
}
(( $+functions[_dfx__ping_commands] )) ||
_dfx__ping_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ping commands' commands "$@"
}
(( $+functions[_dfx__beta__project_commands] )) ||
_dfx__beta__project_commands() {
    local commands; commands=(
'import:Imports canister definitions from another project, as remote canisters' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx beta project commands' commands "$@"
}
(( $+functions[_dfx__pull_commands] )) ||
_dfx__pull_commands() {
    local commands; commands=()
    _describe -t commands 'dfx pull commands' commands "$@"
}
(( $+functions[_dfx__quickstart_commands] )) ||
_dfx__quickstart_commands() {
    local commands; commands=()
    _describe -t commands 'dfx quickstart commands' commands "$@"
}
(( $+functions[_dfx__wallet__redeem-faucet-coupon_commands] )) ||
_dfx__wallet__redeem-faucet-coupon_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet redeem-faucet-coupon commands' commands "$@"
}
(( $+functions[_dfx__remote_commands] )) ||
_dfx__remote_commands() {
    local commands; commands=(
'generate-binding:Generate bindings for remote canisters from their .did declarations' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx remote commands' commands "$@"
}
(( $+functions[_dfx__identity__remove_commands] )) ||
_dfx__identity__remove_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity remove commands' commands "$@"
}
(( $+functions[_dfx__wallet__remove-controller_commands] )) ||
_dfx__wallet__remove-controller_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet remove-controller commands' commands "$@"
}
(( $+functions[_dfx__identity__rename_commands] )) ||
_dfx__identity__rename_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity rename commands' commands "$@"
}
(( $+functions[_dfx__replica_commands] )) ||
_dfx__replica_commands() {
    local commands; commands=()
    _describe -t commands 'dfx replica commands' commands "$@"
}
(( $+functions[_dfx__info__replica-port_commands] )) ||
_dfx__info__replica-port_commands() {
    local commands; commands=()
    _describe -t commands 'dfx info replica-port commands' commands "$@"
}
(( $+functions[_dfx__info__replica-rev_commands] )) ||
_dfx__info__replica-rev_commands() {
    local commands; commands=()
    _describe -t commands 'dfx info replica-rev commands' commands "$@"
}
(( $+functions[_dfx__canister__request-status_commands] )) ||
_dfx__canister__request-status_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister request-status commands' commands "$@"
}
(( $+functions[_dfx__schema_commands] )) ||
_dfx__schema_commands() {
    local commands; commands=()
    _describe -t commands 'dfx schema commands' commands "$@"
}
(( $+functions[_dfx__canister__send_commands] )) ||
_dfx__canister__send_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister send commands' commands "$@"
}
(( $+functions[_dfx__wallet__send_commands] )) ||
_dfx__wallet__send_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet send commands' commands "$@"
}
(( $+functions[_dfx__wallet__set-name_commands] )) ||
_dfx__wallet__set-name_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet set-name commands' commands "$@"
}
(( $+functions[_dfx__identity__set-wallet_commands] )) ||
_dfx__identity__set-wallet_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity set-wallet commands' commands "$@"
}
(( $+functions[_dfx__cache__show_commands] )) ||
_dfx__cache__show_commands() {
    local commands; commands=()
    _describe -t commands 'dfx cache show commands' commands "$@"
}
(( $+functions[_dfx__ledger__show-subnet-types_commands] )) ||
_dfx__ledger__show-subnet-types_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger show-subnet-types commands' commands "$@"
}
(( $+functions[_dfx__canister__sign_commands] )) ||
_dfx__canister__sign_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister sign commands' commands "$@"
}
(( $+functions[_dfx__sns_commands] )) ||
_dfx__sns_commands() {
    local commands; commands=(
'config:Subcommands for working with configuration' \
'deploy:Subcommand for creating an SNS' \
'import:Subcommand for importing sns API definitions and canister IDs' \
'download:Subcommand for downloading SNS WASMs' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx sns commands' commands "$@"
}
(( $+functions[_dfx__canister__start_commands] )) ||
_dfx__canister__start_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister start commands' commands "$@"
}
(( $+functions[_dfx__start_commands] )) ||
_dfx__start_commands() {
    local commands; commands=()
    _describe -t commands 'dfx start commands' commands "$@"
}
(( $+functions[_dfx__canister__status_commands] )) ||
_dfx__canister__status_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister status commands' commands "$@"
}
(( $+functions[_dfx__canister__stop_commands] )) ||
_dfx__canister__stop_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister stop commands' commands "$@"
}
(( $+functions[_dfx__stop_commands] )) ||
_dfx__stop_commands() {
    local commands; commands=()
    _describe -t commands 'dfx stop commands' commands "$@"
}
(( $+functions[_dfx__toolchain_commands] )) ||
_dfx__toolchain_commands() {
    local commands; commands=(
'install:Install or update given toolchain(s)' \
'uninstall:Uninstall toolchain(s)' \
'list:List installed toolchains' \
'default:Set default toolchain or get current default toolchain' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx toolchain commands' commands "$@"
}
(( $+functions[_dfx__ledger__notify__top-up_commands] )) ||
_dfx__ledger__notify__top-up_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger notify top-up commands' commands "$@"
}
(( $+functions[_dfx__ledger__top-up_commands] )) ||
_dfx__ledger__top-up_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger top-up commands' commands "$@"
}
(( $+functions[_dfx__ledger__transfer_commands] )) ||
_dfx__ledger__transfer_commands() {
    local commands; commands=()
    _describe -t commands 'dfx ledger transfer commands' commands "$@"
}
(( $+functions[_dfx__toolchain__uninstall_commands] )) ||
_dfx__toolchain__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'dfx toolchain uninstall commands' commands "$@"
}
(( $+functions[_dfx__canister__uninstall-code_commands] )) ||
_dfx__canister__uninstall-code_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister uninstall-code commands' commands "$@"
}
(( $+functions[_dfx__canister__update-settings_commands] )) ||
_dfx__canister__update-settings_commands() {
    local commands; commands=()
    _describe -t commands 'dfx canister update-settings commands' commands "$@"
}
(( $+functions[_dfx__upgrade_commands] )) ||
_dfx__upgrade_commands() {
    local commands; commands=()
    _describe -t commands 'dfx upgrade commands' commands "$@"
}
(( $+functions[_dfx__wallet__upgrade_commands] )) ||
_dfx__wallet__upgrade_commands() {
    local commands; commands=()
    _describe -t commands 'dfx wallet upgrade commands' commands "$@"
}
(( $+functions[_dfx__identity__use_commands] )) ||
_dfx__identity__use_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity use commands' commands "$@"
}
(( $+functions[_dfx__sns__config__validate_commands] )) ||
_dfx__sns__config__validate_commands() {
    local commands; commands=()
    _describe -t commands 'dfx sns config validate commands' commands "$@"
}
(( $+functions[_dfx__wallet_commands] )) ||
_dfx__wallet_commands() {
    local commands; commands=(
'addresses:Print wallet'\''s address book' \
'add-controller:Add a wallet controller' \
'authorize:Authorize a wallet custodian' \
'balance:Get the cycle balance of the selected Identity'\''s cycles wallet' \
'controllers:List the wallet'\''s controllers' \
'custodians:List the wallet'\''s custodians' \
'deauthorize:Deauthorize a wallet custodian' \
'name:Get wallet name' \
'redeem-faucet-coupon:Redeem a code at the cycles faucet' \
'remove-controller:Remove a wallet controller' \
'send:Send cycles to another cycles wallet' \
'set-name:Set wallet name' \
'upgrade:Upgrade the wallet'\''s Wasm module to the current Wasm bundled with DFX' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'dfx wallet commands' commands "$@"
}
(( $+functions[_dfx__info__webserver-port_commands] )) ||
_dfx__info__webserver-port_commands() {
    local commands; commands=()
    _describe -t commands 'dfx info webserver-port commands' commands "$@"
}
(( $+functions[_dfx__identity__whoami_commands] )) ||
_dfx__identity__whoami_commands() {
    local commands; commands=()
    _describe -t commands 'dfx identity whoami commands' commands "$@"
}

_dfx "$@"
